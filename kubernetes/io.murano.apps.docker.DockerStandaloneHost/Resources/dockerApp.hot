resources:
  $dockerResourceName:
    type: "DockerInc::Docker::Container"
    properties:
      docker_endpoint: $tcp://{dockerServer}:2375
      image: $dockerImage
      env:
        $dockerEnv
      port_specs:
        $dockerAppPorts
      port_bindings:
        $dockerPortBindings
      volumes:
        $dockerVolumes
